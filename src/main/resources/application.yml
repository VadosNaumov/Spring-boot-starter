server:
  port: 8080

spring:
  h2:
    console:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include:
          - beans
          - health
          - metrics
          - env

